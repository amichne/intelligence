name: git-ci-operations
version: 0.1.0
description: "Git and CI operations workflow for evidence-backed changes: goals, TDD, Git hygiene, PRs, GitHub Actions, releases, and shell-safe automation."
author: Amichne
license: Apache-2.0
type: hybrid
targets: [claude, codex]
metadata:
  role: "git-ci-operations"
  scope: "Goal framing, TDD, local Git hygiene, PR lifecycle, GitHub Actions triage, release workflow repair, and shell-safe automation."
  primitiveSource: package-owned-apm
